import Link from 'next/link'; import { notFound } from 'next/navigation'; import { works } from '@/lib/content';
export default async function WorkDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const work=works.find(x=>x.slug===slug); if(!work) return notFound(); return <main><header className="site-header"><Link href="/" className="wordmark">K—MINHWA</Link><nav><Link href="/works">WORKS</Link><Link href="/artist">ARTIST</Link></nav><span className="edition">WORK / 雲住 金慧震</span></header><section className="detail"><div className="detail-image art-image" style={{backgroundImage:`url(${work.image})`}} role="img" aria-label={work.imageAlt}/><div className="detail-copy"><p className="eyebrow">{work.year} / {work.medium}</p><h1>{work.title}</h1><p>{work.description}</p><Link href="/works" className="text-link">← BACK TO WORKS</Link></div></section></main>}
